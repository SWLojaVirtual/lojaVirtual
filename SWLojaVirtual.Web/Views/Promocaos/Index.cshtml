@model IEnumerable<SWLojaVirtual.Dominio.Entidades.Promocao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Leve)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pague)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Leve)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pague)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPromocao }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPromocao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPromocao })
        </td>
    </tr>
}

</table>

<div>
    @Html.ActionLink("Lista de Produtos", "Index", "Produtoes")
</div>